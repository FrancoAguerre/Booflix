window.onload = function () {
    switch (window.location.hash) {
        case "#dateInverted":
            showToast("La fecha 'Desde' no puede ser mayor a la fecha 'Hasta'");
            break;
 
  }
}